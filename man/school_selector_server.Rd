% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/school-selector.R
\name{school_selector_server}
\alias{school_selector_server}
\title{Cascading school selector server logic}
\usage{
school_selector_server(
  input,
  session,
  prefix,
  default_csv,
  required_cols = c("state", "county", "school_name", "school_id", "vaccination_rate",
    "num_students"),
  on_school_select,
  max_file_size_mb = 5,
  max_rows = 10000
)
}
\arguments{
\item{input}{The Shiny \code{input} object.}

\item{session}{The Shiny \code{session} object.}

\item{prefix}{Character prefix for input IDs, typically the model name
(e.g. \code{"measles"}).}

\item{default_csv}{Path to the default CSV file (already resolved, e.g.
via \code{\link[base]{system.file}}).}

\item{required_cols}{Character vector of required CSV column names.}

\item{on_school_select}{Callback with signature
\code{function(school_row, session, prefix)} invoked when a valid
school is selected.  Use this to populate model-specific form fields.}

\item{max_file_size_mb}{Maximum upload size in megabytes (default 5).}

\item{max_rows}{Maximum rows allowed in an uploaded CSV (default 10000).}
}
\value{
The \code{\link[shiny]{reactiveVal}} holding the current school
data frame (invisibly).
}
\description{
Registers Shiny observers for a state -> county -> school cascading
dropdown set, including CSV upload with validation and a reset button.
The function is parameterized by \code{prefix} so that multiple models
can each have independent selector instances.
}
\details{
Expected input IDs (created by the caller's UI):
\code{{prefix}_state_selector}, \code{{prefix}_county_selector},
\code{{prefix}_school_selector}, \code{{prefix}_school_csv},
\code{{prefix}_reset_school_data}.
}
